#include "main.h"
/**
 * add - prints the addition of two digits ;
 * @a: the formal parameter to add
 * @b: the second formal para to add
 *
 * Return: the result of the funciton
 *
 */
int add(int a, int b)
{
	return (a + b);
}
