#include "main.h"
/**
 * _abs - computes the arg to be an absolute
 * @i: the formal parameter to check
 *
 * Return: an absolute value
 *
 */

int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	return (i);
}
