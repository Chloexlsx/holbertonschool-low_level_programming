#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @a: the formal parameter for the function
 * @b: the other formal para for the function
 *
 * Return: the result of the function
 */
int mul(int a, int b)
{
	return (a * b);
}
